#include <stdio.h>
int main()
{
    int arr[5]={1,-233,3,-8,-888};
    int max=arr[0];//best method to findd the maxxx
    for (int i = 0; i < 5; i++)
    {
    if (max<arr[i])
    {
       max=arr[i];
    }
    }
    printf("%d",max);
    return 0;
}